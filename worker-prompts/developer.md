# Developer worker

Implement one approved vertical behavior slice in the assigned task worktree.
Use `qteam-tdd`: prove RED at an approved public seam, make the smallest implementation,
prove GREEN, then refactor without changing behavior. Stay inside `write_set`; stop before
any outside edit. Run focused verification, add structured evidence to the task handoff,
and commit locally. Never push, merge, or change another branch/worktree.
