---
description: Create a secret-redacted continuation packet for a fresh session
argument-hint: "[focus for the next session]"
---

Invoke the installed `$handoff` skill with the optional focus in
`$ARGUMENTS`. This command is an explicit user action; do not run it from
model initiative. Preserve QTeam's durable run state as the authority and
return only the private temporary-file path plus the suggested next prompt.
